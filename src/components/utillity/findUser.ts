import { store } from "../../app/store";

export const findUser = () => {
  //   const user = store.getState().userReducer.find((f) => {
  //     f.id === store.getState().loginUser;
  //   });
  //   return user ? user : [];
};
